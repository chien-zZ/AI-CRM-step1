import { readFile } from "node:fs/promises";
import { Ajv2020 } from "ajv/dist/2020.js";
import { describe,expect,it } from "vitest";
describe("Task Center event contract",()=>{it("accepts a business-neutral stable-route lifecycle event and rejects URLs",async()=>{const schema=JSON.parse(await readFile(new URL("../../../../contracts/events/task-projection-lifecycle.v1.schema.json",import.meta.url),"utf8")) as object;const validate=new Ajv2020({strict:true,validateFormats:false}).compile(schema);const value={eventId:"43000000-0000-4000-8000-000000000001",sourceType:"workflow",sourceTaskId:"task.synthetic",sourceVersion:1,occurredAt:"2026-07-26T00:00:00.000Z",status:"open",deepLink:{appId:"workbench",routeId:"task-detail"}};expect(validate(value),JSON.stringify(validate.errors)).toBe(true);expect(validate({...value,deepLink:{appId:"https://outside.invalid",routeId:"task-detail"}})).toBe(false);});});
